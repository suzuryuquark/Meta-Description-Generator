# 開発・コントリビューションガイド

この文書は、Meta Description Generatorに対するコード、テスト、文書、リリース作業の共通ルールを定義します。

## 1. 基本原則

- 1つの変更では1つの目的だけを扱います。
- 利用者から見える動作を変える場合は、テストと文書も同じ変更に含めます。
- 既存の保存データ、テンプレート、履歴との後方互換性を維持します。
- 既存コードの整理と機能追加は、可能な限り別のコミットまたはPRに分けます。
- APIキー、個人情報、取得した非公開コンテンツをリポジトリやログへ残しません。
- Webページの内容とAIの応答は、いずれも信頼できない外部入力として扱います。

## 2. 開発環境

### 必要な環境

- Windows
- Python 3.11以上
- Git
- Gemini APIを使う手動テストに限り、Google AI StudioのAPIキー

通常の自動テストはGemini APIキーなしで実行できなければなりません。

### セットアップ

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -r requirements-dev.txt
```

実行時依存関係は `requirements.txt`、開発用依存関係は `requirements-dev.txt`、各ツールの動作設定は `pyproject.toml` を正本とします。

## 3. ブランチと変更単位

新しいバージョンの開発は、必ず `develop` ブランチから専用の開発用ブランチを切り出して行います。`main` または `develop` ブランチ上で直接開発を開始してはいけません。

開発用ブランチの命名規則:

```text
dev/v<開発するバージョン番号>
```

バージョン番号は `X.Y.Z` 形式とします。例えば、Version 1.4.0の開発用ブランチは次の名前になります。

```text
dev/v1.4.0
```

開発開始時の基本操作:

```powershell
git switch develop
git switch -c dev/v1.4.0
```

- 開発用ブランチは、そのバージョンで予定する機能、修正、テスト、文書更新をまとめる単位とします。
- 開発中に見つかった次バージョン向けの変更を、現在の開発用ブランチへ混在させません。
- 開発完了後はレビューと品質ゲートを通過させてから `develop` へ統合します。
- `develop` 以外を起点に開発用ブランチを作成してしまった場合は、実装を進める前に起点を修正します。

## 4. コミットメッセージ

Conventional Commitsを基本とします。

```text
<type>(<任意のscope>): <要約>
```

使用する主なtype:

- `feat`: 利用者向け機能
- `fix`: 不具合修正
- `refactor`: 外部動作を変えない構造変更
- `test`: テストのみ
- `docs`: 文書のみ
- `chore`: 開発環境、依存関係、ビルド作業
- `perf`: 性能改善
- `security`: セキュリティ改善

例:

```text
feat(gemini): 利用可能なモデルを選択できるようにする
fix(history): 修正後のdescriptionを履歴へ反映する
```

- 要約は日本語または英語で統一された文章にします。
- 何を変更したかだけでなく、必要に応じて本文に理由と移行上の注意を書きます。
- 互換性を壊す変更には `BREAKING CHANGE:` を記載します。
- 自動生成物だけのコミットとソース変更を混在させません。

## 5. Pythonコーディング規約

`pyproject.toml` を機械的な規約の正本とします。

- Blackでフォーマットします。
- Ruffで構文、import順、代表的な不具合パターンを検査します。
- 1行は原則100文字以内とします。
- 新規の公開クラス、公開関数、サービス境界には型ヒントを付けます。
- 新規コードでは広すぎる `except Exception` を避け、処理可能な例外を具体的に捕捉します。
- エラーメッセージは、利用者向け表示と診断情報を分離します。
- 長時間またはブロッキングする処理をUIイベントループ上で直接実行しません。
- UIコントロールを `controls[9]` のような位置で参照せず、名前付き属性またはメソッドを使用します。
- マジックナンバーは名前付き定数または設定モデルへ移します。
- 外部APIの生レスポンスをUIへ直接渡さず、アプリ内データモデルへ変換します。

### 既存コードの扱い

規約導入時点の既存コードには未整形・未型付けの箇所があります。

- 変更したPythonファイルは、その変更範囲を新規違反のない状態にします。
- 大量のフォーマット変更は、機能変更と分けた専用PRで行います。
- v1.4.0の機能実装前に、リポジトリ全体のBlack/Ruff基準を確立するベースライン変更を行います。
- 既存の無関係な問題を見つけても、現在の変更へ混在させず課題として記録します。

## 6. アーキテクチャ規約

依存関係は原則として次の向きにします。

```text
UI → アプリケーション処理 → サービス → 外部SDK・ストレージ
                 ↓
             データモデル
```

- `ui/` はFletの表示とユーザー操作を担当します。
- `services/` はGemini、HTTP取得、保存、エクスポートなどの外部境界を担当します。
- データモデルはUIコントロールや外部SDKの型へ依存させません。
- UIからGemini SDKや `requests` を直接呼びません。
- サービスからFletコントロールを直接操作しません。
- GeminiモデルIDは呼び出し元から明示的に渡し、サービス内で暗黙に上書きしません。
- 利用不可モデルから別モデルへの自動フォールバックは行わず、利用者へ再選択を求めます。

## 7. セキュリティと外部入力

### APIキー

- APIキーをソース、テストデータ、スクリーンショット、ログへ記録しません。
- APIキーを含む可能性がある例外やURLを、そのまま表示・記録しません。
- 実APIテストでは環境変数またはOS資格情報ストアを使います。
- テスト用のダミー値は、実在するキーに見えない明確な文字列にします。

### URLとWebコンテンツ

- URLは利用前にスキーム、ホスト、解決先を検証します。
- リダイレクト後も同じ検証を行います。
- タイムアウト、最大レスポンスサイズ、Content-Typeを制限します。
- Webページ本文に書かれた命令を、アプリやシステムへの指示として扱いません。
- ローカル、プライベート、リンクローカルアドレスへのアクセスは既定で拒否します。

## 8. 保存データと互換性

- 保存形式を変更する場合は `schema_version` を更新します。
- 移行処理は複数回実行しても結果が壊れない冪等な実装にします。
- 旧形式の履歴、テンプレート、設定を読み込めるテストを追加します。
- 読み込み失敗時にデータを空値で上書きしません。
- 破壊的な移行が必要な場合は、バックアップと復元方法を先に定義します。

## 9. テスト規約

### テストの分類

- `tests/unit/`: 外部通信を行わない単体テスト
- `tests/integration/`: サービス間の結合テスト。外部APIは原則モック
- `tests/fixtures/`: HTML、JSONなどの固定テストデータ
- `tests/test_startup.py`: Fletアプリの起動スモークテスト
- `tests/test_live_gemini.py`: 明示実行専用のGemini実APIテスト

### 必須ケース

変更内容に応じて、少なくとも次を確認します。

- 正常系
- 空値と境界値
- 外部サービスのタイムアウト、429、5xx
- 不正なGemini応答
- 保存データの旧形式
- UI処理中の例外後にボタンや進捗状態が復元されること

不具合修正では、可能な限り先に不具合を再現する回帰テストを追加します。

### 標準検証コマンド

```powershell
python -m compileall -q . -x "(build|dist|\.git)"
python -m pytest
python -m ruff check .
python -m black --check .
python -m mypy .
```

Black/Ruffの全体ベースラインが確立するまでは、フォーマットとlintを少なくとも変更対象ファイルに対して実行します。テストを実行できない場合は、理由と未検証範囲を変更説明に記載します。

実APIテストは通常テストと分離し、明示的なマーカーまたは環境変数がある場合だけ実行します。CIやレビューで第三者のAPIキーを要求してはいけません。

## 10. 文書と変更履歴

次の場合はREADMEまたは関連文書を更新します。

- UI操作や設定項目が変わる
- 必要なPython・依存ライブラリが変わる
- 保存データや移行手順が変わる
- パッケージ作成手順が変わる
- GeminiモデルやAPIの利用条件が変わる

利用者へ影響する変更は `CHANGELOG.md` の未リリース版へ記録します。実装計画と実装が異なった場合は、計画書を現状に合わせて更新します。

## 11. PR・レビュー

PR説明には次を含めます。

- 目的と背景
- 主な変更
- 対象外とした事項
- テスト結果
- UI変更時のスクリーンショット
- 保存データ、API、セキュリティへの影響
- 既知の制約と後続作業

レビューでは次を優先します。

1. データ消失、秘密情報漏えい、任意URL取得などの安全性
2. 既存機能と保存データの互換性
3. エラー時のUI状態と復旧可能性
4. テスト可能性と責務分離
5. 可読性とスタイル

## 12. Definition of Done

変更は次を満たした時点で完了とします。

- 受け入れ条件を満たしている
- 必要なテストが追加され、成功している
- 構文、lint、フォーマット、型チェックを実施している
- APIキーなどの秘密情報が含まれていない
- 保存データの互換性を確認している
- README、CHANGELOG、実装計画を必要に応じて更新している
- Windows上のFlet起動を確認している
- リリース対象ではWindows向けパッケージのスモークテストを完了している
- バージョン実装の完了後、リリース版の
  `dist/MetaDescriptionGenerator.exe` を生成している

## 13. バージョニングとリリース

バージョンはSemantic Versioningを基本とします。

- Major: 互換性を壊す変更
- Minor: 後方互換な機能追加
- Patch: 後方互換な不具合・セキュリティ修正

リリース時には次を確認します。

1. 全品質ゲート
2. `CHANGELOG.md`
3. READMEの機能・モデル・依存関係
4. アプリ内バージョン表示
5. クリーン環境での依存関係インストール
6. Windows向けパッケージ作成
7. パッケージ版の起動、生成、保存、終了

### Windowsリリース実行ファイルの生成

全機能の実装と品質ゲートが完了してから、プロジェクト直下で次を実行します。
バージョン番号は対象リリースに合わせます。リリース専用環境を作り直すことで、
開発ツールがexeへ混入することを防ぎます。

```powershell
.\scripts\build_release.ps1 -Version 1.5.2 -RecreateEnvironment
```

依存関係は `requirements-build.lock` で固定します。既定の`Deduplicated`プロファイルは、
アプリ実行時に不要な `setuptools`、`rich`、`pygments` などを収録せず、さらに
Fletランタイムと内容が一致するFlet固有のルートDLLを除外します。除外は
`packaging/windows_release.spec`のAnalysis段階で行い、完成したexeを直接加工しません。

スクリプトは成果物が62,000,000 bytes以下であること、Flet固有重複と除外対象が
未収録であること、正規配置側が維持されていること、exeのバージョン情報が一致することを
検証します。問題の切り分けには`-OptimizationProfile Extended -MaximumBytes 90000000`
を指定し、v1.5.1相当の方式へ戻せます。

成果物は `dist/MetaDescriptionGenerator.exe` とします。生成後はパッケージ版で起動、
設定保存、通常生成、正常終了を確認します。容量ゲートは機能テストの代わりにはなりません。
`dist/`、`.venv-build/`、自動生成されたspecファイルはGitへコミットしません。
`packaging/windows_release.spec`はレビュー対象のソースファイルとしてコミットします。
