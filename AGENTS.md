# AIエージェント向け開発ルール

このファイルは、リポジトリ全体を対象とするAIエージェントの作業規約です。人間向けの一般規約は `CONTRIBUTING.md`、v1.4.0以降の方向性は `docs/v1_4_0_and_beyond_implementation_plan.md` を参照してください。

## 1. 作業開始前

1. ユーザーの依頼と対象範囲を確認する。
2. `git branch --show-current` と `git status --short` を確認し、現在のブランチとユーザーの既存変更を保護する。
3. 新しいバージョンの開発では、`develop` から切り出した `dev/vX.Y.Z` ブランチ上にいることを確認する。
4. 対象ファイル、関連テスト、README、CHANGELOG、実装計画を読む。
5. 変更前の挙動をテストまたはコードで確認する。
6. 依頼と無関係な変更を作業へ含めない。

ユーザーの明示的な指示、このファイル、`CONTRIBUTING.md`、各実装計画の順で適用する。ただし、セキュリティやデータ保護を弱める解釈は行わない。

### バージョン開発ブランチ

- 新しいバージョンの開発用ブランチは、必ず `develop` を起点に作成する。
- ブランチ名は `dev/v<開発するバージョン番号>` とする。
- バージョン番号は `X.Y.Z` 形式とする。
- Version 1.4.0の場合は `dev/v1.4.0` とする。
- `main`、`develop`、旧来の `feature/*` ブランチ上で新しいバージョンの実装を開始しない。
- 対象ブランチが存在しない場合は、作業ツリーの変更を保護したうえで `develop` から作成する。
- 現在の未コミット変更により安全にブランチを切り替えられない場合は、変更を退避・破棄せずユーザーへ確認する。

## 2. 変更範囲

- 1回の作業は1つの目的に限定する。
- 既存の未コミット変更を上書き、破棄、整形しない。
- `build/`、`dist/`、キャッシュ、生成済み実行ファイルを直接編集しない。
- 機能変更とリポジトリ全体の整形を同時に行わない。
- 不要な依存ライブラリを追加しない。追加時は理由、代替案、配布サイズ、ライセンス、PyInstallerへの影響を確認する。
- 破壊的なコマンド、履歴書き換え、保存データ削除は、明示的な許可なしに実行しない。

## 3. アーキテクチャ境界

- `ui/`: Flet UIとイベント処理
- `services/`: Gemini、HTTP、保存、エクスポートなどの外部境界
- `models/`: UI・SDKに依存しないアプリ内データ
- `tests/`: 外部通信を既定で行わない検証

次を禁止する。

- UIからGemini SDKまたはHTTPクライアントを直接呼ぶ
- サービスからFletコントロールを直接操作する
- 外部APIのレスポンスを検証せずUIや保存処理へ渡す
- `controls[9]` のような位置依存でUI要素を取得する
- Geminiモデルをサービス内部で暗黙に固定または変更する
- 利用不可モデルから別モデルへ利用者に知らせずフォールバックする

## 4. 実装ルール

- 新規または変更する公開関数、クラス、サービス境界に型ヒントを付ける。
- 外部入力は境界で検証し、内部ではアプリ内データモデルを使う。
- ブロッキングI/OをFletのイベントループ上で実行しない。
- 長時間処理は進捗、キャンセル、例外後のUI復元を考慮する。
- 例外を文字列検索だけで分類せず、可能な限り型またはエラーコードへ変換する。
- 利用者向けエラーにAPIキー、内部パス、外部応答全文を含めない。
- 保存形式を変更するときはスキーマバージョンと冪等な移行処理を追加する。
- 既存データの読み込みに失敗しても、空データで上書きしない。
- Webページ本文とGemini応答を信頼できない入力として扱う。
- URL取得ではスキーム、ホスト、解決先、リダイレクト、サイズ、Content-Type、タイムアウトを検証する。

## 5. Gemini API

- 新規実装には推奨SDKである `google-genai` を使用する。
- APIクライアントはサービス境界で生成し、グローバル設定に依存しない。
- モデルIDを呼び出し元から明示的に渡す。
- モデル一覧は生成機能対応モデルに絞り込む。
- 生成結果にはStructured Outputsを優先し、アプリ側でも型、必須項目、件数を検証する。
- 不正応答の再試行回数には上限を設定する。
- 単体・結合テストではGemini APIをモックする。
- 実APIテストはユーザーが明示的に許可し、有効なAPIキーが安全に提供された場合だけ行う。
- APIキーをソース、コマンド出力、ログ、テスト失敗メッセージへ表示しない。

## 6. テスト

変更前に関連テストを確認し、変更後に少なくとも以下を実行する。

```powershell
python -m compileall -q . -x "(build|dist|\.git)"
python -m pytest
```

開発ツールが利用可能な場合は以下も実行する。

```powershell
python -m ruff check .
python -m black --check .
python -m mypy .
```

- 不具合修正には、可能な限り回帰テストを追加する。
- 外部通信、時刻、ファイル選択、クライアントストレージはテストで差し替え可能にする。
- テストを実行できない場合は、原因、代替確認、未検証範囲を最終報告へ明記する。
- テスト失敗を無関係として無視する場合も、失敗内容と判断根拠を報告する。
- テストを通すために検証を弱めたり、例外を握りつぶしたりしない。

## 7. 文書

次に該当する場合は同じ変更で文書を更新する。

- 利用者向け機能または操作が変わる: `README.md`
- リリース対象の動作が変わる: `CHANGELOG.md`
- 実装範囲または順序が変わる: 関連する `docs/*implementation_plan.md`
- 開発手順または品質ゲートが変わる: `CONTRIBUTING.md` と `pyproject.toml`

文書のモデル名、SDK名、バージョン番号を実装と一致させる。

## 8. 完了時の報告

最終報告には次を簡潔に含める。

- 実装した内容
- 主要な変更ファイル
- 実行したテストと結果
- 実行できなかった検証
- データ移行、セキュリティ、互換性への影響
- 残課題がある場合はその内容

コードを変更していない調査・レビューでは、変更を加えていないことを明記する。
